function array(rows, columns){
    var arr = new Array(rows)
    for(var i=0; i<rows; i++){
        arr[i] = new Array(columns);
    }
    return arr;
}
var arr =array(0,0);
arr.push([2,2])
console.log(arr);

class Solution{
    function(m,n,arr){
        
    }
}